{
  "name": "dotworks",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && netlify deploy --prod --dir=dist",
    "deploy:preview": "vite build && netlify deploy --dir=dist"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "netlify-cli": "^26.1.0",
    "vite": "^6.0.0"
  }
}
